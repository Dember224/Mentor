<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>

    <h1>Hi,
        <%= User.find_by_id(Message.find_by_from(User.find_by_email(@user).id).to).name %>
    </h1>
    <p>You have a new message from <%= User.find_by_email(@user).name %></p>
    <p><%= @sent_message %></P>
    <p> To respond login at <a href="Temelachus.herokuapp/login">Telemachus</a></p>
  <img src= "https://image.ibb.co/gNfkpx/logo.png" />
  </body>
  </html>
